CREATE TABLE `zerocup`.`poi` (
  `id` INT NOT NULL,
  `NAME` VARCHAR(45) NOT NULL,
  `URL` VARCHAR(100) NOT NULL,
  `DESCRIPTION` TEXT NULL,
  `LONGITUDE` DOUBLE NULL,
  `LATITUDE` DOUBLE NULL,
  PRIMARY KEY (`id`));
